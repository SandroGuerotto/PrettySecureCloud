package ch.psc.domain.file;

/**
 * For better readability of the code, this enum is used to
 * define the encryption state of a {@link PscFile}.
 * <p>
 * Currently, no further functionality provided by this enum class.
 */
public enum EncryptionState {
  ENCRYPTED, DECRYPTED
}
